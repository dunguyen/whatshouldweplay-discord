language: node_js
node_js:
  - 14

services:
  - docker

script:
  - npm run lint
  - npm run build
  - npm run coverage

cache:
  directories:
    - /node_modules

deploy:
  provider: script
  script: bash docker_push.sh
  on:
    branch: master